#include "draw.h"
#include "draw.hpp"

int draw_open()
{
	// return g_draw.open();
}

void draw_point(int x, int y)
{
	// g_draw.point(x, y);
}

void draw_line(int sx, int sy, int ex, int ey)
{
	// g_draw.line(sx, sy, ex, ey);
}

void draw_clear()
{
	// g_draw.clear();
}

void draw_close()
{
	// g_draw.close();
}